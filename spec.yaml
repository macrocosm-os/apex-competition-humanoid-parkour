schema: apex.competition.v1
id: humanoid_parkour
version: 0.4.0
display_name: "Humanoid Parkour"
kind: solo
process_type: cpu

# Measured under these exact limits: referee peaks at 560 MiB (36% of 1.5Gi), player at 30 MiB.
# Referee memory does not scale with submission quality — a policy that survives longer runs more
# steps, and steps do not allocate — so this is close to the true ceiling, not a baseline figure
# that better solutions will grow into.
resources:
  cpu_limit: 1
  mem_limit: 1.5Gi
  gpu_count: 0

# Built, pushed, and keyless-cosign-signed by this repo's release workflow (see
# .github/workflows/release.yml). `image` is the PLAYER image (loads + serves the miner's ONNX
# policy).
#
# !! STALE — both digests are from the v0.3.3 release run, which predates per-round course
# generation. The referee image MUST be rebuilt and both digests re-pinned before this version is
# registered, or the platform will run the old fixed-course referee under a new version number.
# The platform pins by digest and rejects tags.
image:
  ref: ghcr.io/macrocosm-os/apex-competition-humanoid-parkour-v2-player
  digest: sha256:b5552649c857a111b9d44323d1801149933587b372307288ee94f8b2d4d473d5

# ONNX artifact: the miner submits a policy graph, not code. The player image validates the
# interface at load time (obs [batch,104] + state_in [batch,256] -> action [batch,12] +
# state_out [batch,256], all float32); there is no code to screen and no Layer-2 screen image.
#
# 25 MB, set against measurement rather than taste. Every Unitree reference locomotion policy
# (G1, H1, H1-2) is 0.13-0.14 MB, so this is ~180x the class of policy this task actually needs,
# and it still fits a 6-layer d=256 transformer over a history window (~5M params, ~20 MB).
#
# It is NOT a compute limit: measured single-threaded, a 5.5M-param net runs 0.35 ms/step, so a
# full-survival run (96,000 inferences) costs 34 s against ~640 s of slack in the referee budget.
# Even 74 MB would fit here. The cap exists because surplus capacity buys nothing on a
# proprioception + height-scan control problem.
#
# NOTE: this cap used to be justified partly by the evaluation suite being FIXED — "spare parameters
# invite memorising 24 instances instead of learning to walk". That argument no longer applies now
# that the course is drawn per round (env.course.generate_course), because there is no fixed layout
# left to memorise. The cap is retained on the first ground alone and should be revisited: a
# generalising terrain policy plausibly wants more capacity than a memorising one.
#
# The architecture itself is NOT constrained; only the tensor signature is.
submission:
  artifact_type: onnx
  max_size_mb: 25
  target_path: /app/submission.onnx

screening:            # Layer-1 generic weights validator (onnx artifact_type)
  max_size_mb: 25
  min_weight_bytes: 10000        # rejects near-empty graphs / constant policies
  max_code_weight_ratio: 0.1

input_schema:
  $ref: ./input.schema.json

defaults:
  baseline_score: 0.0
  # !! STALE — MUST BE RE-MEASURED BEFORE THIS VERSION SHIPS !!
  #
  # 0.2007 was measured against the FIXED course, which no longer exists: the course is now drawn
  # from the round's master seed (env.course.generate_course), so a given policy's score varies with
  # the round. Host-side spot checks of the stock-walker baseline at n=3 came out 0.1852 (seed 0) and
  # 0.1827 (seed 1) — not comparable to the figure below, but enough to show it has moved.
  #
  # Re-measure as the MEAN over >= 20 round seeds, and record the spread, because this number is now
  # a random variable rather than a constant. It is also the fallback score-to-beat when a round's
  # score chain starts empty, so a figure measured on one lucky draw biases takeover for everyone.
  # Still measure IN THE REFEREE IMAGE ON NATIVE amd64 (.github/workflows/measure-baseline.yml): a
  # host-side measurement and an arm64 build of this same image disagree by 0.04% and 0.12%, against
  # a 1% takeover margin.
  #
  # Expect round-to-round variance to be the dominant sizing problem, not architecture spread. The
  # earlier v0.2.0 design — also per-round-seeded courses — measured sigma_round = 0.0304 at N=120
  # against a 1% margin, i.e. 17x too noisy (variance_baseline_N120_image.json at tag v0.2.0). With
  # ONE course per round that gets worse, not better, because the round's score is a single draw.
  baseline_raw_score: 0.2007
  round_length_in_days: 2
  submission_reveal_days: 5      # trained policies carry real R&D; protect them
  lower_is_better: false

entrypoints:
  # The PLAYER sandbox: serves the ONNX policy over gym_v1; the referee drives it one
  # observation at a time. timeout_s must cover the whole evaluation (24 instances x <=4000
  # control steps): measured 67 s for the baseline, ~258 s for a policy that survives every step.
  evaluate:
    command: ["python", "/app/launch.py", "--port", "8000"]
    network_disabled: false   # the referee must reach the player on the per-job network
    allow_internet: false
    # Deliberately LONGER than referee.timeout_s (900): the player's clock starts earlier
    # (image start + readiness polling) and it must outlive the referee. If the player is
    # killed first, the referee's remaining instances score player_error — a zero the
    # submission did not earn.
    timeout_s: 1200
    http_api:
      port: 8000
      readiness_path: /health
      protocol: gym_v1

# The scorer sandbox: MuJoCo physics, the G1 model, the course, termination gates, scoring.
# NO generate_round entrypoint, deliberately, and it must stay that way: the referee derives the
# whole suite from the per-round SEED, so there is nothing to run. Declaring one would make the
# platform's spec round generation RAISE (apex-mvp _prepare_spec_next_round -> "not implemented
# yet (APEX-101)"), which would stop rounds rolling entirely.
referee:
  protocol: gym_v1
  image:
    ref: ghcr.io/macrocosm-os/apex-competition-humanoid-parkour-v2-referee
    digest: sha256:14902b2913d7f6f6d681c83fceeffcee5181de447d3b4536dcf0b8c6c9af4c86
  timeout_s: 900

signature:
  cosign_identity: https://github.com/macrocosm-os/apex-competition-humanoid-parkour-v2/.github/workflows/release.yml
  cosign_issuer: https://token.actions.githubusercontent.com
